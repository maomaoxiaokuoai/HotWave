# ===== 环境变量（全部可选） =====

# PostgreSQL 连接串：
# 本地开发 / 自托管部署时配置，用于持久化主题偏好与热搜缓存。
# Cloudflare 部署【无需配置】，应用自动降级为 localStorage + 内存缓存。
# DATABASE_URL=postgresql://user:password@host:5432/database

# X（推特）热搜抓取的 Bearer Token（可选）：
# 不配置则使用 x.com 网页端内置的公开访客 token。
# TWITTER_BEARER=
