# Cloudflare Workers 部署配置（OpenNext 适配器生成 .open-next/worker.js）
name = "hotwave"
main = ".open-next/worker.js"
compatibility_date = "2025-05-01"

# ===== 可选环境变量 =====
# 无需数据库：偏好设置存浏览器 localStorage，热搜缓存用内存。
# X（推特）抓取的 Bearer Token（可选，不配置则使用内置公开访客 token）
# [vars]
# TWITTER_BEARER = "你的token"
